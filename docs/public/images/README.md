# Image placeholders

Reserved public-release captures:

- workspace overview;
- body-free Messages queue and authorized thread;
- composer hold and final-backspace release.

Use synthetic agents, messages, paths, and repositories. Crop or redact host
chrome and verify that no message body, token, private path, or third-party
content is exposed before committing an image.
