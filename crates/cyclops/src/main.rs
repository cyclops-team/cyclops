//! cyclops CLI entry point. M0 implementation lands here.

fn main() {
    eprintln!(
        "cyclops {}: not yet implemented (M0 in progress)",
        env!("CARGO_PKG_VERSION")
    );
    std::process::exit(1);
}
