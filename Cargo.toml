[workspace]
resolver = "2"
members = [
    "src/cyclops-proto",
    "src/cyclops-manifest",
    "src/cyclops-tmux",
    "src/cyclops-ledger",
    "src/cyclops-theme",
    "src/cyclops-ui",
    "src/cyclopsd",
    "src/cyclops",
    # Test-only: the isolated tmux server and its teardown rule, in one
    # place because the sites that need it span crates and test kinds.
    "tests/testrig",
    "src/cyclops-workspace",
]

# The production landing page for usecyclops.dev lives at website/ as a
# read-only branding reference. It is not part of the Rust workspace.
exclude = ["website"]

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
regex = "1"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
uuid = { version = "1", features = ["v4"] }
libc = "0.2"

cyclops-proto = { path = "src/cyclops-proto" }
cyclops-manifest = { path = "src/cyclops-manifest" }
cyclops-tmux = { path = "src/cyclops-tmux" }
cyclops-ledger = { path = "src/cyclops-ledger" }
cyclops-theme = { path = "src/cyclops-theme" }
cyclops-ui = { path = "src/cyclops-ui" }
cyclops-testrig = { path = "tests/testrig" }
cyclops-workspace = { path = "src/cyclops-workspace" }

[profile.release]
lto = "thin"
